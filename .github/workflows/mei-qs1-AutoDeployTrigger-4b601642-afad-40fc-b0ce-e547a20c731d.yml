name: Trigger auto deployment for mei-qs1

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ master ]
    paths:
    - '**'
    - '.github/workflows/mei-qs1-AutoDeployTrigger-4b601642-afad-40fc-b0ce-e547a20c731d.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.MEIQS1_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.MEIQS1_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.MEIQS1_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          registryUrl: meiacr.azurecr.io
          registryUsername: ${{ secrets.MEIQS1_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.MEIQS1_REGISTRY_PASSWORD }}
          containerAppName: mei-qs1
          resourceGroup: mei-env
          imageToBuild: meiacr.azurecr.io/mei-qs1:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


